/**
 * Enumeration of errors in the inputs
 */
export enum CtrlError {
  PATTERN = 'pattern',
  REQUIRED = 'required',
  MAX = 'max',
  MAX_LENGTH = 'maxlength',
  MIN = 'min',
  MIN_LENGTH = 'minlength'
}
